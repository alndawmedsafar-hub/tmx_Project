// TMX Termux Universe - File 3: Commander VIP Office
const tmxOffice = [
    { c: "tmx --master --login", m: "چوونە ناو ناوەندی بڕیاری TMX" },
    { c: "pkg install tor torsocks -y", m: "دابەزاندنی تۆڕی تۆر بۆ ونی" },
    { c: "pkg install proxychains-ng -y", m: "بەستنەوەی چەندین ئایپی پێکەوە" },
    { c: "git clone https://github.com/T-M-X/Shadow-Script", m: "سکرێپتی سێبەری TMX" },
    { c: "pkg install screen fetch -y", m: "ڕێکخستنی شاشەی فەرماندە" },
    { c: "tmx-key-gen --bit 8192", m: "دروستکردنی کلیلی ئەمنی زەبەلاح" },
    { c: "pkg install neofetch figlet toilet -y", m: "بۆ دیزاین و جوانی تێرمۆکس" }
];

for (let k = 8; k <= 150; k++) {
    tmxOffice.push({
        c: `tmx-vip-cmd --execute-silent --key-2010aa --id-${k}`,
        m: `فەرمانی دەگمەنی فەرماندەی گشتی ژمارە ${k}`
    });
}

function checkOffice() {
    let p = prompt("TMX SECURITY KEY:");
    if(p === "2010aa") {
        document.getElementById('office-win').style.display = 'flex';
        renderData('office-body', tmxOffice);
    } else {
        alert("ACCESS DENIED!");
    }
}
